SPIEGAZIONE DETTAGLIATA DEL METODO paintComponent()

───────────────────────────────────────────────
METODO:
───────────────────────────────────────────────
@Override
protected void paintComponent(Graphics g) {
    super.paintComponent(g);

    Graphics2D g2d = (Graphics2D) g.create();
    g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);

    int w = getWidth();
    int h = getHeight();

    // 🌈 Gradiente base (dall'alto verso il basso)
    Color startColor = new Color(Costanti.COLOR1_HEX).brighter();
    Color endColor = new Color(Costanti.COLOR2_HEX).darker();
    GradientPaint base = new GradientPaint(0, 0, startColor, 0, h, endColor);
    g2d.setPaint(base);
    g2d.fillRect(0, 0, w, h);

    // ✨ Effetto glossy (riflesso bianco nella parte superiore)
    GradientPaint gloss = new GradientPaint(
        0, 0, new Color(255, 255, 255, 100),     // bianco semi-trasparente
        0, h * 0.4f, new Color(255, 255, 255, 0) // svanisce dopo il 40% dell’altezza
    );
    g2d.setPaint(gloss);
    g2d.fillRect(0, 0, w, (int)(h * 0.4)); // applica solo nella parte alta

    g2d.dispose();
}

───────────────────────────────────────────────
DESCRIZIONE DETTAGLIATA:
───────────────────────────────────────────────

1️⃣  **Override di paintComponent**
    - Questo metodo viene ridefinito per disegnare manualmente lo sfondo del pannello.
    - È chiamato automaticamente da Swing ogni volta che il componente deve essere ridisegnato.
    - Il metodo `super.paintComponent(g)` viene sempre richiamato per evitare di cancellare
      altri elementi (ad esempio testo o componenti figli) già gestiti da JPanel.

───────────────────────────────────────────────

2️⃣  **Creazione di Graphics2D**
    - `Graphics2D g2d = (Graphics2D) g.create();`
    - Crea una copia dell’oggetto grafico originale.
      In questo modo eventuali modifiche (es. gradienti o trasformazioni)
      non influenzano altri disegni.
    - Graphics2D è la versione avanzata di Graphics,
      che consente effetti più moderni come gradienti e trasparenze.

───────────────────────────────────────────────

3️⃣  **Antialiasing**
    - `g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);`
    - Attiva l’antialiasing, ovvero la “morbidezza” dei bordi,
      riducendo l’effetto seghettato di linee e sfumature.

───────────────────────────────────────────────

4️⃣  **Larghezza e altezza**
    - `int w = getWidth();`
    - `int h = getHeight();`
    - Ottiene la larghezza e l’altezza correnti del pannello,
      necessarie per disegnare sfondi adattabili alla dimensione.

───────────────────────────────────────────────

5️⃣  **Creazione del gradiente di base**
    - Si definiscono due colori:
        `startColor = new Color(Costanti.COLOR1_HEX).brighter();`
        `endColor = new Color(Costanti.COLOR2_HEX).darker();`
      → Il primo è un colore più chiaro (nella parte superiore),
        il secondo è più scuro (in basso).
    - `GradientPaint base = new GradientPaint(0, 0, startColor, 0, h, endColor);`
      → Crea un gradiente verticale (coordinate x costanti, y da 0 a h)
        che sfuma dal colore chiaro a quello scuro.
    - `g2d.setPaint(base);`
      → Imposta questo gradiente come “pennello”.
    - `g2d.fillRect(0, 0, w, h);`
      → Riempie tutto il pannello con il gradiente.

───────────────────────────────────────────────

6️⃣  **Creazione dell’effetto glossy (riflesso)**
    - Dopo aver disegnato il gradiente base,
      si aggiunge un secondo livello semitrasparente sopra di esso.
    - `GradientPaint gloss = new GradientPaint(0, 0, new Color(255,255,255,100), 0, h * 0.4f, new Color(255,255,255,0));`
      → Crea un gradiente verticale che parte da un bianco semi-trasparente
        (alpha 100 su 255 ≈ 40% di opacità) e svanisce fino alla trasparenza
        al 40% dell’altezza del pannello.
    - `g2d.fillRect(0, 0, w, (int)(h * 0.4));`
      → Disegna il riflesso solo nella parte alta del pannello.
      Questo simula la luce che colpisce una superficie lucida, come il vetro.

───────────────────────────────────────────────

7️⃣  **Chiusura**
    - `g2d.dispose();`
    - Libera le risorse grafiche create con `create()`,
      evitando perdite di memoria o effetti indesiderati.

───────────────────────────────────────────────

📘  RISULTATO VISIVO:
───────────────────────────────────────────────
✔ Sfondo con sfumatura verticale morbida.
✔ Effetto tridimensionale (parte superiore chiara, parte inferiore scura).
✔ Riflesso bianco realistico nella parte alta → effetto "vetroso" o "glossy".
✔ Adattabile automaticamente alle dimensioni del pannello.
───────────────────────────────────────────────
